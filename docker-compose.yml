version: '3'

services:
  postgres:
    image: postgres:15-alpine
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=db_admin
      - POSTGRES_PASSWORD=admin_pw
      - POSTGRES_DB=pic_up
